# effect/ 操作音

和 `voice/` 里的「碰 / 杠 / 胡了 / 自摸胡」分开放，方便单独替换。
英文名和中文名都认，支持 `.wav` / `.ogg` / `.mp3`。

| 文件名 | 中文名（备选） | 什么时候响 |
|---|---|---|
| `fumble` | `摸牌` | 玩家摸牌（含杠后的补摸） |
| `drop` | `打出` | 玩家打出一张牌 |
| `impact` | `悬停` | 鼠标移到自己的手牌上 |

注：代码在 `voice/`、`effect/` 和主目录里都会找，文件放哪一层都能响。
